class NumberModel{
  String ? image;
  String ? textInJapanese;
  String ? textInEnglish;

  NumberModel({
    required this.image ,
    required this.textInJapanese,
    required this.textInEnglish,
});
}